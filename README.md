# PoE Timeless Jewels

《流亡黯道》永恆珠寶查詢工具，提供單點 Seed 查詢、天賦樹範圍檢視、詞綴反查，以及交易連結。
目前版本：`v0.3.1`

## 功能
- 永恆珠寶 / 將軍 / Seed 查詢
- 依詞綴與權重反查 Seed
- 國際服與台服交易搜尋按鈕
- 本機收藏珠寶清單
- 收藏 JSON 匯入 / 匯出

## 資料來源
- 官方台服網站：[pathofexile.tw](https://pathofexile.tw/)
- PoEDB 永恆珠寶資料參考：
  - [繁中](https://poedb.tw/tw/Timeless_Jewel)
  - [英文](https://poedb.tw/us/Timeless_Jewel)
- 原始專案（致謝）：[Vilsol/timeless-jewels](https://github.com/Vilsol/timeless-jewels)
- Extracted data: [Vilsol/go-pob-data](https://github.com/Vilsol/go-pob-data)

## 更新紀錄

### 2026-04-04 UI 版面優化（節省空間）
- 「將軍」選單改為與「珠寶」同列顯示（在右側），減少垂直空間占用。
- 「全選 / 全選強力天賦 / 全選小天賦 / 全部排除」改到「依詞綴反查」模式切換列右側，縮短反查操作動線。
- 「新增目標詞綴」改為文字與選擇器同列（選擇器在右側）。
- 每條詞綴的「最低數量 / 權重」改為置於詞綴右側，且標籤在左、輸入框在右。
- 「最低總權重」改為標籤在左、輸入框在右，減少欄位高度。

### 2026-04-04 中英詞綴錯位修正（抗性）
- 修正特定抗性詞綴出現「中文與英文不同步」的問題（例如中文顯示火焰、英文顯示冰冷）。
- 翻譯流程改為優先依英文模板做精準中譯，再回退至官方模板與 ID 對照，避免錯位。
- 修正 `zh_tw.ts` 中 `statDescriptions` 的抗性 key：`150` 改為 `+#% 閃電抗性`。

### 2026-04-04 天賦樹頁右下角資訊調整
- 移除天賦樹右下角「前往 GitHub 原始專案」連結。
- 改為顯示版本號（目前為 `v0.3.1`），避免與主頁版本資訊不一致。

### 2026-04-04 版本號更新（v0.3.1）
- 專案版本號由 `v0.3.0` 更新為 `v0.3.1`。
- 前端 `package.json` 與首頁顯示版本已同步調整。
- 前端版本字串改為共用常數（`frontend/src/lib/version.ts`），避免多頁面手動同步遺漏。

### 2026-04-04 詞綴中英校對（3.27）
- 以本地 `go-pob-data-main/data/3.27/raw`、`tree`、`stat_translations` 作為參考來源進行比對。
- 永恆珠寶相關詞綴（159 條）改為優先採用官方繁中模板，避免翻譯詞不一致。
- 重新檢查結果：繁中模板缺漏 0、繁中模板不一致 0、英文模板缺漏 0。

### 2026-04-04 部署策略調整（最新）
- 停用 Firebase Hosting GitHub Actions 自動部署，改為手動部署。
- 手動部署指令：`firebase deploy --only hosting --project poe-timeless-jewels`
- 版本號更新：`v0.3.0`

### 2026-04-04 致謝與來源補充
- 本專案目前維護版 GitHub：`https://github.com/Lucifer631217/PoE-Timeless-Jewels.git`
- 本專案基於原作者專案修改：`https://github.com/Vilsol/timeless-jewels.git`
- 感謝原作者 **Vilsol** 開源此專案，提供重要的基礎與資料流程。

### 2026-04-04 交易條件語意修正（本次）
- 將前端命名從 `TradeSaleMode` 統一調整為 `TradeCondition`，避免誤解成「販售型式」。
- 交易條件選項改為對齊官網語意：`即刻購買`、`面對面（聯盟上線）`、`面對面（上線）`、`不限`。
- 國際服查詢新增狀態映射：`面對面（聯盟上線）` 會套用 `status=onlineleague`，其餘條件維持 `status=online`。
- 台服維持相容：兩種面對面條件皆映射為 `sale_type=facetoface`。
- 修正國際服 `sale_type` 映射為 `buyout/facetoface`，避免 `即刻購買` 被錯誤解讀為面對面交易。
- 調整狀態映射：`即刻購買` 與 `不限` 改用 `status=any`，僅面對面條件使用 `online/onlineleague`。
- 台服相容修正：`即刻購買` 改為 `status=online`，避免台服將查詢誤判為非即刻購買條件。

### 2026-04-04 交易修正補充
- 交易條件改為單一模式：即刻購買 / 面對面交易 / 不限，避免舊版雙開關互相覆蓋。
- 國際服 sale_type 對應：即刻購買 -> priced、面對面交易 -> unpriced。
- 台服 sale_type 對應：即刻購買 -> buyout、面對面交易 -> facetoface。
- 聯盟下拉排除 Solo Self-Found (SSF) 自力聯盟，避免選到不可交易聯盟。
- 反查結果卡與收藏珠寶卡共用同一組交易設定（模式與當前選擇聯盟）。
- 修正聯盟翻譯大小寫比對：MIRAGE / HARDCORE MIRAGE / RUTHLESS MIRAGE / HC RUTHLESS MIRAGE 等全大寫名稱也會正確顯示中文。

### 2026-04-04 交易修正（本次）
- 修正交易條件套用邏輯：即刻購買 與 面對面交易 同時勾選時改為不套 sale type（不限）。
- 重構交易詞綴查詢：移除 disabled filter 方案，改為穩定的 count + min:1 分組查詢，避免詞綴條件被市集忽略。
- 台服詞綴查詢改為雙軌：優先將軍 pseudo stat，並保留台服 stat id fallback，提升詞綴代入成功率。
- 聯盟值改為優先使用 id（非顯示名稱），並在交易 URL path segment 做 encodeURIComponent，降低聯盟路由錯誤。

### 2026-04-04 更新
- 修正：切換珠寶時不再重置已選的技能樹插槽。
- 調整：詞綴改為中英分色顯示，英文以獨立顏色呈現。
- 修正：詞綴翻譯模板會依數值條件選取，降低中英對照不一致。
- 調整：反查預設改為全選強力天賦（小天賦預設排除）。
- 文案：將「顯著天賦」統一改為「強力天賦」。
- 優化「珠寶 / 將軍」選單可讀性，放大字級與高度。
- 修正部分詞綴中英對照錯配（優先依英文模板翻譯）。
- 收藏珠寶改為右側獨立視窗，並在左側標題列加入「收藏珠寶」切換按鈕。

### 2026-04-03 修正紀錄（收藏與反查）
- 修正收藏資料存在時，反查結果不易顯示的版面問題：反查結果區塊改為先顯示，收藏區塊移到後方。
- 收藏珠寶改為預設收合，新增 `展開收藏/收合收藏` 按鈕。
- 左上角面板按鈕改為「圖示 + 文字」，收合後也可清楚辨識「展開面板」。
- 交易條件文案調整：`一口價` 改為 `即刻購買`、`面交` 改為 `面對面交易`。
- 詞綴顯示改為中英文雙語（結果卡、Seed 詞綴列表、收藏快照與重點詞綴）。

### 2026-04-03
- 新增「收藏珠寶」功能，可把重要珠寶加入自訂列表。
- 收藏項目支援流派、重要屬性、預估價值編輯。
- 收藏項目保留國際服 / 台服交易按鈕。
- 新增收藏 JSON 匯入與匯出。
- 網站開啟時改為預設選取當季聯盟。
- README 重整，移除先前衝突標記與損壞內容。

### 2026-04-04 資料同步流程更新（3.28 相容）
- 新增 `update_assets.ps1`（Windows PowerShell 版本），可直接同步技能樹與 go-pob-data 資料。
- 支援自動偵測 `TreeVersion`（預設會嘗試 `3.28.0`、`3.28.0b`...）。
- 當 `go-pob-data` 尚未提供目標版本（例如 3.28）時，可用 `-AllowFallbackToLatest` 自動回退至最新可用版本（目前為 3.27）。
- 實測指令：`powershell -ExecutionPolicy Bypass -File .\update_assets.ps1 -GameVersion 3.28 -AllowFallbackToLatest -SkipGenerate`。

### 2026-04-04 功能更新（將軍可選「任何」）
- `tree` 頁面的將軍下拉選單新增「任何」選項，可在不限定將軍名稱的情況下操作。
- 在「任何」模式下，反向搜尋會自動遍歷該珠寶所有將軍並合併結果。
- 搜尋結果卡片會顯示每筆結果實際對應的將軍名稱，交易連結會帶入該筆結果的將軍。
- 交易查詢支援 `ANY_CONQUEROR`，可用單次查詢覆蓋同珠寶的所有將軍統計條件。
- 交易查詢先改為只使用 `status.option` 控制條件（`instant_buyout -> securable`），暫時不送 `filters.trade_filters.sale_type`。
- 交易條件精簡：移除「面對面(上線)」與「不限」，目前僅保留「秒出」與「面對面(聯盟上線)」。

### 2026-04-04 版本號更新（v0.3.0）
- 專案版本號由 v0.2.0 更新為 v0.3.0。
- 前端 `package.json` 與首頁顯示版本已同步調整。

### 2026-04-04 介面文案修復
- 修正 `tree` 頁面因字串污染導致的 `????` 顯示問題，恢復主要操作文案為繁體中文。
- 保留既有交易條件精簡邏輯（僅「秒出」與「面對面（聯盟上線）」）。
- 收藏相關提示訊息與按鈕文案恢復為繁中，提升可讀性與操作一致性。

### 2026-04-04 交易條件文案與台服「任何將軍」修正
- 交易條件文案調整為官方用語：`即刻購買`、`面對面交易（聯盟在線）`。
- 修正台服在「任何將軍」模式下的詞綴條件代入：改為優先使用各將軍 `pseudo` 詞綴 ID，避免落入舊版單一 fallback ID 導致查詢不準確。

### 2026-04-04 收藏摘要與英文詞綴著色修正
- 移除收藏編輯視窗「結果摘要」與收藏卡片中 `snapshot`/`stats` 的固定條數裁切，改為完整顯示。
- 「選擇要反查的詞綴」區塊中的雙語詞綴顯示改為支援英文關鍵字著色。
- 收藏珠寶「重要屬性」與快照詞綴改為同樣套用英文關鍵字著色，與反查結果視覺一致。

